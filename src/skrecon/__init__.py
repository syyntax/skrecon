"""skrecon / birdseye — authorized recon orchestrator for Stormkeep engagements.

The safety-critical core (scope, guard, config, model, audit, store, engagement)
is stdlib-only and importable without any third-party dependency. Only the CLI
layer (`skrecon.cli`) requires `typer`.
"""

__version__ = "0.1.0"
